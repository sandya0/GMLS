<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.GMLS" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Basic Android theme attributes -->
        <item name="android:colorPrimary">@color/red</item>
        <item name="android:colorPrimaryDark">@color/primary_variant</item>
        <item name="android:colorAccent">@color/secondary</item>
        
        <!-- Window attributes -->
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        
        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/on_surface</item>
    </style>
</resources>